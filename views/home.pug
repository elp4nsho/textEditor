doctype html
html
    head
        link(href="./assets/estilos.css" rel='stylesheet')

    body

div
    <!--iframe(src="./assets/index.html")-->
    button(onclick="cambiarURL()")="cambiar"
    div(style="display:none;")
        h1(class="titulo")="hola"
        a(href="/apic")="apic"
        a(href="/oli")="oli"
        a(href="/admin")="admin"
        a(href="/viewer")="viewer"
        input#text(type=text)
        button(onclick="hola()")="escribir"

    div.contenido
        div.contenidoArchivos
            h3="Files"
            button(onclick="crearDirectorio(prompt().toString())")="📁"
            button(onclick="crearArchivo(prompt())")="➕"
            button(onclick="irAHome()")="🏠"
            br
            small#ruta=directorioActual
            ul#directorios
                li(onClick="obtenerDirectorios('../')")="../"
                each file in files
                    li(onClick=`${file.isDirectory() ? 'obtenerDirectorios("' + file.name + '")' : 'obtenerArchivo("' + file.name + '")'}` class=`${file.isDirectory() ? "directorio" : "archivo"}` )=file.name
                    button.eliminar(onclick=`eliminarDirectorio("${file.name}")`)="X"
        div.contenidoEditor
            input(value="data.txt" onkeyup=`renombrarArchivo()`)#archivoEditando
            button#guardar(onclick="escribirArchivo()")="Guardar"
            textarea#archvioContenido=archivoLeido


script( src="./assets/js.js")
